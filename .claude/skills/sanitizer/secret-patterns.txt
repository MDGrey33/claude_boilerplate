# Secret detection patterns (one regex per line).
# Comments start with #. Blank lines ignored.
# Sources: detect-secrets, gitleaks, trufflehog common patterns. Add user-specific additions below.
# Apply case-sensitive unless (?i) prefix given.
# When a match fires, redact to <REDACTED:category> and suggest env var replacement.

# ── Anthropic ──
sk-ant-api03-[A-Za-z0-9_-]{80,}
sk-ant-[A-Za-z0-9_-]{80,}

# ── OpenAI ──
sk-[A-Za-z0-9]{20,}T3BlbkFJ[A-Za-z0-9]{20,}
sk-proj-[A-Za-z0-9_-]{40,}

# ── GitHub ──
ghp_[A-Za-z0-9]{36,255}
gho_[A-Za-z0-9]{36,255}
ghu_[A-Za-z0-9]{36,255}
ghs_[A-Za-z0-9]{36,255}
ghr_[A-Za-z0-9]{36,255}
github_pat_[A-Za-z0-9_]{82}

# ── GitLab ──
glpat-[A-Za-z0-9_-]{20,}
glptt-[A-Za-z0-9_-]{40,}

# ── AWS ──
AKIA[0-9A-Z]{16}
ASIA[0-9A-Z]{16}
aws_secret_access_key\s*=\s*[A-Za-z0-9/+=]{40}
aws_session_token\s*=\s*[A-Za-z0-9/+=]{40,}

# ── Google Cloud ──
AIza[A-Za-z0-9_-]{35}
ya29\.[A-Za-z0-9_-]+
"type":\s*"service_account"
"private_key":\s*"-----BEGIN
"client_email":\s*"[^"]+@[^"]+\.iam\.gserviceaccount\.com"

# ── Slack ──
xox[baprs]-[A-Za-z0-9-]{10,}
https://hooks\.slack\.com/services/T[A-Z0-9]+/B[A-Z0-9]+/[A-Za-z0-9]+

# ── Stripe ──
sk_live_[A-Za-z0-9]{24,}
rk_live_[A-Za-z0-9]{24,}
pk_live_[A-Za-z0-9]{24,}

# ── Generic JWT ──
eyJ[A-Za-z0-9_-]+\.eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+

# ── Private key headers ──
-----BEGIN (RSA |DSA |EC |OPENSSH |PGP |ENCRYPTED )?PRIVATE KEY-----
-----BEGIN OPENSSH PRIVATE KEY-----

# ── Database URLs with embedded credentials ──
(?i)(postgres|postgresql|mysql|mongodb|redis)://[^:\s]+:[^@\s]+@[^\s]+

# ── Generic token-assignment patterns ──
(?i)(api[_-]?key|api[_-]?secret|access[_-]?token|auth[_-]?token|bearer[_-]?token|secret[_-]?key|private[_-]?key)\s*[:=]\s*["']?[A-Za-z0-9_\-/+=]{20,}

# ── .credentials path reference ──
~/\.credentials/
/Users/[^/\s]+/\.credentials/

# ── Basic-auth URLs ──
(?i)https?://[^:/\s]+:[^@\s]+@

# ── Telegram bot tokens ──
[0-9]{8,10}:[A-Za-z0-9_-]{35}

# ── npm tokens ──
npm_[A-Za-z0-9]{36,}
